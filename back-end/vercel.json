{
    "devCommand": "uvicorn main:app --host 0.0.0.0 --port 8000",
    "builds": [
      {
        "src": "back-end/main:app",
        "use": "@vercel/python"
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "back-end/main:app"
      }
    ]
  }
  